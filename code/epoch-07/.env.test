NODE_ENV=test
# Tests run as the owner; FORCE ROW LEVEL SECURITY makes RLS apply to it too,
# so the hostile suite still proves enforcement.
DATABASE_URL=postgres://trellis:trellis@localhost:5432/trellis
